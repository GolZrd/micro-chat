name: GO

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test-auth:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Set up Go
        uses: actions/setup-go@v4
        with:
          go-version: 1.24
          cache-dependency-path: go.sum
      
      - name: Build Auth Service
        working-directory: ./auth
        run:
          go build -o ./bin -v ./cmd/grpc_server

      - name: Test Auth Service
        working-directory: ./auth
        run:
          go test -v ./...
  
  test-chat-server:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Set up Go
        uses: actions/setup-go@v4
        with:
          go-version: 1.24
          cache-dependency-path: go.sum
      
      - name: Build Chat Server
        working-directory: ./chat-server
        run:
          go build -o ./bin -v ./cmd/grpc_server

      - name: Test Chat Server
        working-directory: ./chat-server
        run: 
          go test -v ./...